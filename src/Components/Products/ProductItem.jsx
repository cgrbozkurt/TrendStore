
// eslint-disable-next-line react/prop-types
const ProductItem = ({product}) => {
  const {name,description,img,price}=procut;
    return (
   
   
   <li>
    <img src={img} alt="" />
    
    </li>
  )
}

export default ProductItem